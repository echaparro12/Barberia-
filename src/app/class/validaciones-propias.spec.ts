import { validacionesPropias } from './validaciones-propias';

describe('CvalidacionesPropias', () => {
  it('should create an instance', () => {
    expect(new validacionesPropias()).toBeTruthy();
  });
});
